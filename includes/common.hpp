#ifndef COMMON_HPP
#define COMMON_HPP

#include <cstring>
#include <iostream>
#include <vector>
#include <algorithm>

std::vector<std::string> split(const std::string& s, const std::string& delim);

#endif // COMMON_HPP